{
  "name": "kg-gap-filler",
  "version": "1.0.0",
  "description": "A sample Obsidian plugin for gap filling",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && cp src/manifest.json dist/ && cp -rf dist/ ./test/.obsidian/plugins/kggapfiller",
    "dev": "ts-node --inspect=9229 src/main.ts"
  },
  "dependencies": {
    "obsidian": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.0.0"
  }
}